spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ai_gym_pro
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5433/ai_gym_pro
    user: postgres
    password: postgres